#include "config.h"

class Solution
{
public:
    int maxProfit(std::vector<int> &prices)
    {
        int profit = 0;

        if (prices.size() == 0) {
            return 0;
        }

        return profit;
    }
};
